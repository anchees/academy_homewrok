<Window x:Class="Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Клиент курса валют"
        Height="600"
        Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="HostBox" Text="127.0.0.1" Width="120"/>
            <TextBox x:Name="PortBox" Text="5000" Width="80" Margin="5,0"/>
            <TextBox x:Name="LoginBox" Text="user" Width="100"/>
            <TextBox x:Name="PasswordBox" Text="123" Width="100" Margin="5,0"/>

            <Button Content="Подключиться"
                    Width="130"
                    Margin="5,0"
                    Click="ConnectButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="CurrencyBox"
                     Text="USD EUR"
                     Width="200"/>

            <Button Content="Отправить запрос"
                    Width="150"
                    Margin="5,0"
                    Click="SendButton_Click"/>

            <Button Content="Отключиться"
                    Width="120"
                    Click="DisconnectButton_Click"/>
        </StackPanel>

        <TextBox x:Name="ResultBox"
                 Grid.Row="2"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"/>
    </Grid>
</Window>
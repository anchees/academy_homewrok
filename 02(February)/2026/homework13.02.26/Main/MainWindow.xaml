<Window x:Class="Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HTTP, SMTP, FTP. Часть 2"
        Height="650"
        Width="900">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Погода" FontSize="18"/>
            <TextBox x:Name="CityBox" Height="30" Margin="0,5"/>
            <Button Content="Показать погоду на 7 дней"
                    Height="30"
                    Click="WeatherButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,15,0,10">
            <TextBlock Text="Фильм" FontSize="18"/>
            <TextBox x:Name="MovieBox" Height="30" Margin="0,5"/>
            <Button Content="Найти фильм"
                    Height="30"
                    Click="MovieButton_Click"/>

            <TextBox x:Name="EmailBox"
                     Height="30"
                     Margin="0,10,0,5"
                     Text="email получателя"/>

            <Button Content="Отправить результат на почту"
                    Height="30"
                    Click="SendEmailButton_Click"/>
        </StackPanel>

        <TextBox x:Name="ResultBox"
                 Grid.Row="2"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>